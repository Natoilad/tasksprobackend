Ось основні команди Docker, які використовуються для роботи з контейнерами:

1. `docker run`: Запускає новий контейнер зі специфічним образом. Наприклад,
   `docker run <ім'я_образу>`.

2. `docker stop`: Зупиняє контейнер, який вже працює. Наприклад,
   `docker stop <ідентифікатор_контейнера>`.

3. `docker start`: Запускає зупинений контейнер. Наприклад,
   `docker start <ідентифікатор_контейнера>`.

4. `docker restart`: Перезапускає контейнер. Наприклад,
   `docker restart <ідентифікатор_контейнера>`.

5. `docker ps`: Показує список активних контейнерів. Для перегляду всіх
   контейнерів, використовуйте `docker ps -a`.

6. `docker images`: Показує список доступних образів.

7. `docker rm`: Видаляє контейнер. Наприклад,
   `docker rm <ідентифікатор_контейнера>`.

8. `docker rmi`: Видаляє образ. Наприклад, `docker rmi <ім'я_образу>`.

9. `docker exec`: Виконує команду в робочому контейнері. Наприклад,
   `docker exec <ідентифікатор_контейнера> <команда>`.

10. `docker build`: Будує новий образ з Dockerfile. Наприклад,
    `docker build -t <ім'я_образу> <шлях_до_контексту>`.

11. `docker pull`: Завантажує образ з реєстру Docker Hub. Наприклад,
    `docker pull <ім'я_образу>`.

12. `docker push`: Відправляє образ в реєстр Docker Hub. Наприклад,
    `docker push <ім'я_образу>`.

13. `docker network`: Управління мережами Docker.

14. `docker volume`: Управління томами Docker.

Це лише кілька основних команд Docker. Щоб дізнатись більше про команди та їх
параметри, ви можете використовувати `docker --help` або переглянути офіційну
документацію Docker.
